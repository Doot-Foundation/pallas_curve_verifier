# Each directory is a Hardhat project on its own

```shell
cd core/
npm install
npx hardhat test
REPORT_GAS=true npx hardhat test
npx hardhat node
```

```shell
cd layerzero_compatible/
npm install
npx hardhat test
REPORT_GAS=true npx hardhat test
npx hardhat node
```
